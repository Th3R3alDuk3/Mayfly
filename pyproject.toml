[project]
name = "Mayfly"
version = "0.1.0"
description = "FastAPI service that manages per-session OpenCode Docker containers"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "docker>=7.0",
    "pydantic-settings>=2.0",
]
